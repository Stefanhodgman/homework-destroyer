[package]
name = "homework-destroyer"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
# Add Wally packages here as needed
# Example: Promise = "evaera/promise@4.0.0"

[dev-dependencies]
# Add development dependencies here

[server-dependencies]
# Server-only dependencies

[place]
shared-packages = "ReplicatedStorage/Packages"
server-packages = "ServerStorage/Packages"
